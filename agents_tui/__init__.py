"""agents-tui — a Textual dashboard over ~20 Claude Code tmux sessions.

Replaces the fzf "agents cockpit". tmux owns the truth (session list,
identity stamps, attention tint, aerospace wid); /tmp/claude-ctx supplies
ctx%; ~/.claude/projects/*.jsonl transcripts supply the last message + age.
"""

__version__ = "1.0.0"
